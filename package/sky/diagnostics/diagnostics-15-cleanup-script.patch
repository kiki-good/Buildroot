diff -Naurr tr69-data-model_orig/apps/cmd/tr069.sh tr69-data-model_new/apps/cmd/tr069.sh
--- tr69-data-model_orig/apps/cmd/tr069.sh	2014-12-04 12:41:40.000000000 +0000
+++ tr69-data-model_new/apps/cmd/tr069.sh	2015-04-15 13:41:00.177237585 +0100
@@ -1,37 +1,21 @@
 #!/bin/sh
-#
-# Start the pace TR069 specific items 
-#
-
 PACEROOT=/opt/pace
 PACEBIN=${PACEROOT}/bin
 PACELIB=${PACEROOT}/lib
 CMDBDIR=/opt/cm
-NORPARTITION=/dev/mtdblock10
-
 export LD_LIBRARY_PATH=${PACELIB}
 PATH=${PATH}:${PACEBIN}
-
 if test -f ${PACEBIN}/iptest && test "$1" == "start"; then
 ${PACEBIN}/iptest > /dev/null 2>&1
 fi
-
 case "$1" in
   start)
     if test ! -d ${CMDBDIR} ; then
         mkdir -p ${CMDBDIR}
     fi
-
-# Mount NOR Partition, so we have persistent data.
-    mount -t jffs2 ${NORPARTITION} ${CMDBDIR}   
-
     if test -f ${PACEBIN}/cmd ; then
         ${PACEBIN}/cmd -s -d ${CMDBDIR} -x
         sleep 1
-		# start init_cm
-		# We need to initialize some data in CM
-		# in order for things to come up
-		# only add data if this is the first time through
 		cmc -c add cmlegacy.mgmt.0 >/dev/null 2>&1
 		if test $? -eq 0 ; then
 			cmc -i <<EOF
@@ -49,11 +33,8 @@
 mapset cmlegacy.mgmt.0.notifyparams "Device.DeviceInfo.SoftwareVersion" "2&Subscriber";
 commit transaction
 EOF
-
 		fi
-		# end init_cm
 	sleep 1
-	# start init_xmpp
 	cmc -c add cmlegacy.xmpp.0 >/dev/null 2>&1
 		if test $? -eq 0 ; then
 			cmc -i <<EOF
@@ -62,11 +43,8 @@
 set cmlegacy.xmpp.0.xmpp_keepalive "60";
 commit transaction
 EOF
-
 		fi
-	# end init_xmpp
 	sleep 1
-	# start init_upload
 	cmc -c add cmlegacy.upload.0 >/dev/null 2>&1
 		if test $? -eq 0 ; then
 			cmc -i <<EOF
@@ -74,11 +52,8 @@
 set cmlegacy.upload.0.upload_supported "1";
 commit transaction
 EOF
-
 		fi
-	# end init_upload
 	sleep 1
-	# start init_epgasdiags
 	cmc -c add cmlegacy.epgasdiags.0 >/dev/null 2>&1
 		if test $? -eq 0 ; then
         	cmc -i <<EOF
@@ -125,22 +100,17 @@
 commit transaction
 EOF
     fi
-	# end init_epgasdiags
     fi
-
     if test -f ${PACEBIN}/mifd ; then
         ${PACEBIN}/mifd -s -x
         sleep 1
     fi
-
     if test -f ${PACEBIN}/cwmd ; then
         ${PACEBIN}/cwmd -s -x
     fi
-
     if test -f ${PACEBIN}/xmppd ; then
         ${PACEBIN}/xmppd
     fi
-
     if test -f ${PACEBIN}/uploadd ; then
         ${PACEBIN}/uploadd
     fi
@@ -153,13 +123,9 @@
     ${PACEBIN}/cwmd -t
     ${PACEBIN}/mifd -p
     ${PACEBIN}/cmd -p
-    umount ${CMDBDIR}
-    killall -9 xmppd
-    killall -9 uploadd
 	;;
   *)
 	echo $"Usage: $0 {start|stop}"
 	exit 1
 esac
-
 exit $?
