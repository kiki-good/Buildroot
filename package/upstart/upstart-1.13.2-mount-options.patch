diff -Naur upstart-1.13.2/init/main.c upstart-1.13.2_mod/init/main.c
--- upstart-1.13.2/init/main.c	2014-08-14 10:35:42.000000000 +0100
+++ upstart-1.13.2_mod/init/main.c	2015-03-23 10:19:19.213210495 +0000
@@ -333,7 +333,7 @@
 
 		if (needs_devtmpfs) {
 			if (system_mount ("devtmpfs", "/dev",
-					  MS_NOEXEC | MS_NOSUID, NULL) < 0) {
+					  MS_RELATIME, NULL) < 0) {
 				NihError *err;
 
 				err = nih_error_get ();
@@ -349,7 +349,7 @@
 				nih_error ("%s: %s", _("Cannot create directory"), "/dev/pts");
 		}
 
-		if (system_mount ("devpts", "/dev/pts", MS_NOEXEC | MS_NOSUID,
+		if (system_mount ("devpts", "/dev/pts", MS_RELATIME,
 				  "gid=5,mode=0620") < 0) {
 			NihError *err;
 
@@ -410,8 +410,7 @@
 		 * ourselves. Also mount /dev/pts to allow CONSOLE_LOG
 		 * to function if booted in an initramfs-less environment.
 		 */
-		if (system_mount ("proc", "/proc",
-				  MS_NODEV | MS_NOEXEC | MS_NOSUID, NULL) < 0) {
+		if (system_mount ("proc", "/proc", MS_RELATIME, NULL) < 0) {
 			NihError *err;
 
 			err = nih_error_get ();
@@ -420,8 +419,7 @@
 			nih_free (err);
 		}
 
-		if (system_mount ("sysfs", "/sys",
-				  MS_NODEV | MS_NOEXEC | MS_NOSUID, NULL) < 0) {
+		if (system_mount ("sysfs", "/sys", MS_RELATIME , NULL) < 0) {
 			NihError *err;
 
 			err = nih_error_get ();
diff -Naur upstart-1.13.2/init/system.c upstart-1.13.2_mod/init/system.c
--- upstart-1.13.2/init/system.c	2014-08-14 10:35:42.000000000 +0100
+++ upstart-1.13.2_mod/init/system.c	2015-03-23 10:20:30.977212337 +0000
@@ -207,7 +207,7 @@
 		return 0;
 
 	/* Mount the filesystem */
-	if (mount ("none", dir, type, flags, options) < 0)
+	if (mount (type, dir, type, flags, options) < 0)
 		nih_return_system_error (-1);
 
 	return 0;
